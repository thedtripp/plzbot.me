# Episode 7 — verification log

| Claim | Primary source | Corroborating source |
|---|---|---|
| HTTP/2's SETTINGS frame (type 0x04) is sent at connection start and defines six standard parameters (header table size, default 4,096 octets; enable-push, default 1; max concurrent streams; initial window size, default 65,535 octets; max frame size, default 16,384 octets; max header list size) | [RFC 9113 §6.5, HTTP/2](https://www.rfc-editor.org/rfc/rfc9113.html) (IETF, primary) — fetched and quoted directly during scripting | — |
| HTTP/2 replaces the HTTP/1.1 request line with four pseudo-headers (`:method`, `:scheme`, `:authority`, `:path`); the spec defines their meaning but does not mandate a transmission order | [RFC 9113 §8.3, HTTP/2](https://www.rfc-editor.org/rfc/rfc9113.html) (IETF, primary) — confirmed directly: "The RFC does not mandate a specific transmission sequence for pseudo-headers... contains no explicit ordering requirement" | Reused from this project's own `public/docs.html` ("HTTP/2 fingerprinting" section), already-reviewed |
| HTTP/2 pseudo-header order is used in practice by some production bot-detection systems as a fingerprinting signal | General, widely-corroborated claim across independent web-scraping/bot-detection industry sources (e.g. scrapfly.io, multiple anti-detect tooling vendors); kept generic in the script (no specific vendor named, no specific per-browser byte sequence asserted) because the exact per-browser ordering claims found during research came from a single class of secondary source and didn't clear the two-source bar for specific sequences | — |
| Node's HTTP/2 "compatibility API" normalizes the four pseudo-headers into a flat, unordered object alongside regular headers before application code sees the request, discarding transmission order; the lower-level "core"/stream API would preserve it | First-party: this project's own `public/docs.html` ("HTTP/2 fingerprinting" section) and `docs/ARCHITECTURE.md`, already-reviewed engineering finding from this project's build | — |
| Mounting a full web framework (Express) on Node's HTTP/2 compatibility API crashed the server reliably on every HTTP/2 request, with a `TypeError: Cannot read properties of undefined (reading 'readable')` inside `_http_incoming.js`; isolated to the framework/compat-API combination (not a bug in application code) via a bare-handler spike that did not crash; fixed by dropping the framework for a hand-rolled router | First-party: this project's own `src/server/http/router.ts` header comment and prior project history/verification (bare http2 handler confirmed stable across repeated requests in `tests/integration/server.test.ts`) | — |
| This project reports HTTP/2 SETTINGS values but explicitly does not report pseudo-header order, citing the compatibility-API limitation above rather than omitting it silently | First-party: `public/docs.html` ("HTTP/2 fingerprinting" section), `src/server/collectors/http2.ts` | — |

No claims in this episode required a "flagged, not used" exclusion.
