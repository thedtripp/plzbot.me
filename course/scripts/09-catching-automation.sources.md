# Episode 9 — verification log

| Claim | Primary source | Corroborating source |
|---|---|---|
| Antoine Vastel (PhD in browser fingerprinting, VP of Research at DataDome) published research showing headless Chrome could be detected via a mismatch between `Notification.permission` (reporting "denied") and `navigator.permissions.query` for the same notifications permission (reporting "prompt") | [Antoine Vastel — "Detecting Chrome headless, new techniques" (2018)](https://antoinevastel.com/bot%20detection/2018/01/17/detect-chrome-headless-v2.html) (primary, the researcher's own published work) | Independent summaries of the same technique corroborate the specific `Notification.permission`/`navigator.permissions.query` mismatch |
| Vastel's headless-Chrome detection research was published across three successive rounds (2017, 2018, 2019) as Chrome patched previously-published techniques | [Vastel — "Detecting Chrome Headless" (2017)](https://arh.antoinevastel.com/bot%20detection/2017/08/05/detect-chrome-headless.html), [round two (2018)](https://antoinevastel.com/bot%20detection/2018/01/17/detect-chrome-headless-v2.html), [round three (2019)](https://antoinevastel.com/bot%20detection/2019/07/19/detecting-chrome-headless-v3.html) — all primary, same author | — |
| `navigator.webdriver` is defined by the W3C WebDriver specification as a read-only property every conforming browser must expose, true under automation control and false otherwise; the original WebDriver spec reached W3C Recommendation status in 2018 | [W3C WebDriver specification](https://www.w3.org/TR/webdriver2/) (primary spec text) | ["WebDriver is now a W3C Recommendation" — W3C Blog (2018)](https://www.w3.org/blog/news/archives/7081) |
| Chromium engineers have made deliberate, publicly-discussed changes to `navigator.webdriver`'s behavior (e.g. ensuring it correctly reports `false` rather than `undefined` when automation is not active) | [Chromium blink-dev — "Intent to Ship: `navigator.webdriver === false` when automation is not enabled"](https://groups.google.com/a/chromium.org/g/blink-dev/c/h-5nQQLs2QU/m/iRVKU4LhAgAJ) (Google/Chromium, primary) | Note: this specific thread concerns `navigator.webdriver`'s own correctness, not the permissions-API mismatch described above — the script says "gaps *like* the one that started this episode" deliberately, to describe the general pattern of Chromium patching automation-detection gaps over time, without claiming this exact thread fixed that exact bug. |
| User-Agent tokens (`HeadlessChrome`, `Puppeteer`, `Selenium`, `PhantomJS`) are a real but trivially-spoofable automation signal; missing Client Hints/Fetch Metadata headers alongside a Chromium UA claim is a stronger consistency signal | Reused from this project's own `public/docs.html` ("Automation detection" section), `src/server/derive/uaDerived.ts` (`KNOWN_AUTOMATION_TOKENS`), and Episode 4's own first-party probe data (the curl-spoofed-as-Chrome example), already-reviewed | — |

No claims in this episode required a "flagged, not used" exclusion.
