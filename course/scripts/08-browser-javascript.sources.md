# Episode 8 — verification log

| Claim | Primary source | Corroborating source |
|---|---|---|
| Canvas fingerprinting draws text/shapes to a hidden `<canvas>` and reads back pixel data, which varies subtly by GPU/driver/OS font rasterizer/anti-aliasing | Reused from this project's own `public/docs.html` ("Rendering fingerprints" section), already-reviewed | [ppc.land — "Explaining canvas fingerprinting"](https://ppc.land/explaining-canvas-fingerprinting/) |
| Englehardt & Narayanan (Princeton, 2016), "Online Tracking: A 1-million-site Measurement and Analysis": canvas fingerprinting found on 1.6% of the top 1 million sites overall, rising to 5.1% among the top 1,000 sites | [Princeton WebTAP / USENIX — the paper itself (PDF)](https://www.usenix.org/sites/default/files/conference/protected-files/satc-webtap.pdf) and [Princeton Web Census project page](https://webtransparency.cs.princeton.edu/webcensus/) (primary, original authors/institution) | Independent reporting summary corroborating the same 1.6%/5.1% figures |
| The same Englehardt/Narayanan research program documented real, deployed audio fingerprinting (via `OfflineAudioContext`) in active use on the web, not merely as a hypothetical technique | Search summary of the paper's findings, corroborated by [arXiv — "A Study of Feasibility and Diversity of Web Audio Fingerprints"](https://arxiv.org/pdf/2107.14201), an independent academic paper on the same technique | — |
| `WEBGL_debug_renderer_info` exposes `UNMASKED_VENDOR_WEBGL`/`UNMASKED_RENDERER_WEBGL`, the actual GPU vendor/renderer strings, intended for debugging; Firefox disables this extension when `privacy.resistFingerprinting` is enabled | [MDN — `WEBGL_debug_renderer_info` extension](https://developer.mozilla.org/en-US/docs/Web/API/WEBGL_debug_renderer_info) | [Khronos Group — WEBGL_debug_renderer_info ratified extension specification](https://registry.khronos.org/webgl/extensions/WEBGL_debug_renderer_info/) (the actual standards body) |
| Audio fingerprinting via `OfflineAudioContext` renders to an in-memory buffer rather than device speakers, and commonly combines an `OscillatorNode` (a mathematically-generated periodic waveform) with a `DynamicsCompressorNode`, whose floating-point DSP output varies subtly across hardware/software audio stacks | Reused from this project's own `public/docs.html`, already-reviewed | Consistent with the arXiv paper above, describing the same oscillator + dynamics-compressor technique |
| Browsers deliberately do not expose an API listing installed fonts; font detection instead infers installed fonts by comparing rendered text metrics (width/height) for a candidate font name against a generic fallback | Reused from this project's own `public/docs.html`, already-reviewed | — |

No claims in this episode required a "flagged, not used" exclusion.
